mutation CreatePost($contents: String!, $title: String!) {
  createPost(contents: $contents, title: $title) {
    id
    createdAt
    updatedAt
    title
    creatorId
    voteStatus
    contentSnippets
    points
  }
}
